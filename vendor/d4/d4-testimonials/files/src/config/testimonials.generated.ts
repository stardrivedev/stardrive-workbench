/**
 * GENERATED FILE (default). Stardrive overwrites this at assembly with any
 * testimonials supplied in the owner's intake. Ships empty so the module
 * compiles standalone, and because a site must never launch with invented
 * reviews attributed to people who do not exist. The /admin editor and the
 * database always take precedence.
 */
import type { Testimonial } from "@/modules/testimonials/types";

export const seedTestimonials: Testimonial[] = [];
