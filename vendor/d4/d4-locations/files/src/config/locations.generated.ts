/**
 * GENERATED FILE (default). Stardrive overwrites this at assembly with the
 * address and hours supplied in the owner's intake. Ships empty so the module
 * compiles standalone, and because guessed opening hours send real people to
 * a locked door. The /admin editor and the database always take precedence.
 */
import type { Location } from "@/modules/locations/types";

export const seedLocations: Location[] = [];
