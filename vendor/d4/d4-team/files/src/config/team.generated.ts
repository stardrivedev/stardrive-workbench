/**
 * GENERATED FILE (default). Stardrive overwrites this at assembly with the
 * team members supplied in the owner's intake. Ships empty so the module
 * compiles standalone, and because inventing staff who do not exist is the
 * one mistake a professional-services site can never recover from. The
 * /admin editor and the database always take precedence.
 */
import type { TeamMember } from "@/modules/team/types";

export const seedTeam: TeamMember[] = [];
