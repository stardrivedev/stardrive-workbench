/**
 * GENERATED FILE (default). Stardrive overwrites this at assembly with any
 * events supplied in the owner's intake. Ships empty so the module compiles
 * standalone, and because an invented event is a real person turning up to an
 * empty room. The /admin editor and the database always take precedence.
 */
import type { SiteEvent } from "@/modules/events/types";

export const seedEvents: SiteEvent[] = [];
