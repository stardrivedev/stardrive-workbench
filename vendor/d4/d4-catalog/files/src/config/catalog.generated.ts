/**
 * GENERATED FILE (default). Stardrive overwrites this at assembly with the
 * AI-written seed products/categories from the owner's intake. Ships empty so
 * the module compiles standalone; the /admin editor and the DB always take
 * precedence.
 */
import type { Product, CatalogCategory } from "@/modules/catalog/types";

export const seedCategories: CatalogCategory[] = [];

export const seedProducts: Product[] = [];
