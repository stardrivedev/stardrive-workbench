/**
 * GENERATED FILE (default). Stardrive overwrites this at assembly with the
 * AI-written seed articles from the owner's intake. Ships empty so the module
 * compiles standalone; the /admin editor and the DB always take precedence.
 */
import type { Article } from "@/modules/insights/types";

export const seedArticles: Article[] = [];
