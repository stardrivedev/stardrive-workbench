/**
 * GENERATED FILE. Written by d4-site-builder during assembly when the build
 * config names a pairing (see pairings.json in d4-site-builder). This
 * checked-in default is the "modern-signal" pairing so the template runs
 * standalone. Do not edit by hand; edits are overwritten on reassembly.
 */
export const pairingId = "modern-signal";

/** Motion signature; the template's motion layer keys off this value. */
export const motionMode = "reveal-fast";

/**
 * True when theme.css carries a validated .dark palette. Gates the header
 * theme toggle and the early theme script; with no dark palette the site is
 * permanently light and no toggle renders.
 */
export const darkMode = true;
