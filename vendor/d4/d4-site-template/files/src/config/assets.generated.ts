/**
 * GENERATED FILE. Stardrive rewrites this at assembly with the customer's
 * uploaded asset compartments (compartment id -> public URL paths). This
 * checked-in default keeps the template fully working standalone.
 */
export const siteAssets: Record<string, string[]> = {};
