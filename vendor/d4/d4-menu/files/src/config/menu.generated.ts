/**
 * GENERATED FILE (default). Stardrive overwrites this at assembly with the
 * menus supplied in the owner's intake. Ships empty so the module compiles
 * standalone, and because an invented dietary marker is the one piece of
 * generated copy that can send someone to hospital. The /admin editor and the
 * database always take precedence.
 */
import type { Menu } from "@/modules/menu/types";

export const seedMenus: Menu[] = [];
