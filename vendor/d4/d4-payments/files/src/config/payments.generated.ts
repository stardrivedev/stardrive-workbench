/**
 * GENERATED FILE (default). Ships empty and stays empty at assembly: a
 * payment link can only be created inside the owner's own Stripe dashboard,
 * so there is nothing here for an intake to supply and nothing that could
 * responsibly be invented. The /admin editor and the database are the only
 * sources.
 */
import type { PaymentItem } from "@/modules/payments/types";

export const seedPaymentItems: PaymentItem[] = [];
